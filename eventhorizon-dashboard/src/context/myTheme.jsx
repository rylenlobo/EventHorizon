import { createTheme, ThemeProvider } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#02072b', // Set your primary color
    },
    secondary: {
      main: '#ff5722', // Set your secondary color
    },
  },
});
